# =============================================================================
# GRVT DeFi Vault - Environment Template
# =============================================================================
# This file stores network credentials only.
# Deployment inputs are defined in JSON5 parameter files under:
#   ignition/parameters/<environment>/*.json5
#
# Fresh proxy deployment example:
#   DEPLOY_PARAMS_FILE=ignition/parameters/testnet/vault-core.json5 \
#   npx hardhat run scripts/deploy/deploy-vault-core.ts --network testnet
# Post-deploy config example:
#   npx hardhat ignition deploy ./ignition/modules/VaultTokenConfig.ts \
#     --network testnet \
#     --parameters ignition/parameters/testnet/vault-token-config.json5
# =============================================================================

# =============================================================================
# Network RPC Endpoints
# =============================================================================
# Testnet RPC URL used by `network: testnet` in hardhat.config.ts
TESTNET_RPC_URL=https://sepolia.infura.io/v3/YOUR_KEY


# =============================================================================
# Deployment Signer Keys
# =============================================================================
# Private key for deployments/operations on the configured testnet.
# Format: 64 hex chars, NO 0x prefix (Hardhat config currently expects raw key).
TESTNET_PRIVATE_KEY=YOUR_PRIVATE_KEY_HEX_NO_0x


# =============================================================================
# Optional Verification Keys
# =============================================================================
# Add explorer API keys here if/when verify flows are enabled.
# ETHERSCAN_API_KEY=


# =============================================================================
# Optional Fork Testing
# =============================================================================
# MAINNET_RPC_URL=https://...
# MAINNET_FORK_BLOCK=22000000

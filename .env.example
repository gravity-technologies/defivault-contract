# Deployment environment: `staging`, `testnet`, or `production`.
GRVT_ENV=

# RPC endpoint used by Hardhat for the active network.
RPC_URL=

# Deployment private key. Use a raw 64-hex string without `0x`.
PRIVATE_KEY=

# Optional explorer API key for contract verification.
ETHERSCAN_API_KEY=
